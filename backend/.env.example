# ============================================================
# LegalAI — ENV EXAMPLE (TEMPLATE)
# ============================================================
# Этот файл — ШАБЛОН окружения.
# Он НЕ используется приложением напрямую.
# Используйте его как контракт для настройки .env
# ============================================================


# ------------------------------------------------------------
# DATABASE
# ------------------------------------------------------------
# Основная база данных LegalAI (SQLite)
# В продакшене используется абсолютный путь
#
# Если переменная не задана — backend не стартует.
#
DATABASE_URL=sqlite:////srv/legal-ai/data/legalai.db


# ------------------------------------------------------------
# LLM: GIGACHAT (PRODUCTION)
# ------------------------------------------------------------
# Если все переменные ниже заданы корректно —
# используется реальная LLM (GigaChat),
# и в логах backend будет:
#
#   GEN=gigachat
#
# Если хотя бы одной переменной нет или ключ неверный —
# backend автоматически переключится в fallback-режим,
# и в логах будет:
#
#   GEN=fallback
#

# OAuth client id из кабинета GigaChat
GIGACHAT_CLIENT_ID=

# OAuth client secret из кабинета GigaChat
GIGACHAT_CLIENT_SECRET=

# OAuth scope (по умолчанию для API)
GIGACHAT_SCOPE=GIGACHAT_API_PERS

# URL авторизации OAuth
GIGACHAT_AUTH_URL=https://ngw.devices.sberbank.ru:9443/api/v2/oauth

# API endpoint GigaChat
GIGACHAT_API_URL=https://gigachat.devices.sberbank.ru/api/v1/chat/completions

# (опционально) CA bundle, если требуется корпоративный сертификат
GIGACHAT_CA_BUNDLE=


# ------------------------------------------------------------
# APPLICATION
# ------------------------------------------------------------
# Окружение приложения
LEGALAI_ENV=production

# Уровень логирования backend
LOG_LEVEL=INFO

